document.addEventListener('DOMContentLoaded', () => {
    const themeSelect = document.getElementById('color-page');

    // Charger les réglages sauvegardés
    const savedTheme = localStorage.getItem('color-page');

    if (savedTheme) {
        document.body.classList.toggle('dark', savedTheme === 'dark');
        themeSelect.value = savedTheme;
    }

    // Appliquer les changements immédiatement quand l'utilisateur change les réglages
    themeSelect.addEventListener('change', () => {
        const selectedTheme = themeSelect.value;
        document.body.classList.toggle('dark', selectedTheme === 'dark');
    });
});
document.addEventListener('DOMContentLoaded', () => {
    const themeSelect = document.getElementById('color-page');

    // Charger les réglages sauvegardés
    const savedTheme = localStorage.getItem('color-page');

    if (savedTheme) {
        document.body.classList.toggle('blueneon', savedTheme === 'blueneon');
        themeSelect.value = savedTheme;
    }

    // Appliquer les changements immédiatement quand l'utilisateur change les réglages
    themeSelect.addEventListener('change', () => {
        const selectedTheme = themeSelect.value;
        document.body.classList.toggle('blueneon', selectedTheme === 'blueneon');
    });
});
document.addEventListener('DOMContentLoaded', () => {
    const themeSelect = document.getElementById('color-page');

    // Charger les réglages sauvegardés
    const savedTheme = localStorage.getItem('color-page');

    if (savedTheme) {
        document.body.classList.toggle('greenneon', savedTheme === 'greenneon');
        themeSelect.value = savedTheme;
    }

    // Appliquer les changements immédiatement quand l'utilisateur change les réglages
    themeSelect.addEventListener('change', () => {
        const selectedTheme = themeSelect.value;
        document.body.classList.toggle('greenneon', selectedTheme === 'greenneon');
    });
});
document.addEventListener('DOMContentLoaded', () => {
    const themeSelect = document.getElementById('color-page');

    // Charger les réglages sauvegardés
    const savedTheme = localStorage.getItem('color-page');

    if (savedTheme) {
        document.body.classList.toggle('redneon', savedTheme === 'redneon');
        themeSelect.value = savedTheme;
    }

    // Appliquer les changements immédiatement quand l'utilisateur change les réglages
    themeSelect.addEventListener('change', () => {
        const selectedTheme = themeSelect.value;
        document.body.classList.toggle('redneon', selectedTheme === 'redneon');
    });
});
document.addEventListener('DOMContentLoaded', () => {
    const themeSelect = document.getElementById('color-page');

    // Charger les réglages sauvegardés
    const savedTheme = localStorage.getItem('color-page');

    if (savedTheme) {
        document.body.classList.toggle('yellowneon', savedTheme === 'yellowneon');
        themeSelect.value = savedTheme;
    }

    // Appliquer les changements immédiatement quand l'utilisateur change les réglages
    themeSelect.addEventListener('change', () => {
        const selectedTheme = themeSelect.value;
        document.body.classList.toggle('yellowneon', selectedTheme === 'yellowneon');
    });
});
document.addEventListener('DOMContentLoaded', () => {
    const themeSelect = document.getElementById('color-page');

    // Charger les réglages sauvegardés
    const savedTheme = localStorage.getItem('color-page');

    if (savedTheme) {
        document.body.classList.toggle('cyanneon', savedTheme === 'cyanneon');
        themeSelect.value = savedTheme;
    }

    // Appliquer les changements immédiatement quand l'utilisateur change les réglages
    themeSelect.addEventListener('change', () => {
        const selectedTheme = themeSelect.value;
        document.body.classList.toggle('cyanneon', selectedTheme === 'cyanneon');
    });
});
document.addEventListener('DOMContentLoaded', () => {
    const themeSelect = document.getElementById('color-page');

    // Charger les réglages sauvegardés
    const savedTheme = localStorage.getItem('color-page');

    if (savedTheme) {
        document.body.classList.toggle('pinkneon', savedTheme === 'pinkneon');
        themeSelect.value = savedTheme;
    }

    // Appliquer les changements immédiatement quand l'utilisateur change les réglages
    themeSelect.addEventListener('change', () => {
        const selectedTheme = themeSelect.value;
        document.body.classList.toggle('pinkneon', selectedTheme === 'pinkneon');
    });
});
document.addEventListener('DOMContentLoaded', () => {
    const themeSelect = document.getElementById('color-page');

    // Charger les réglages sauvegardés
    const savedTheme = localStorage.getItem('color-page');

    if (savedTheme) {
        document.body.classList.toggle('orangeneon', savedTheme === 'orangeneon');
        themeSelect.value = savedTheme;
    }

    // Appliquer les changements immédiatement quand l'utilisateur change les réglages
    themeSelect.addEventListener('change', () => {
        const selectedTheme = themeSelect.value;
        document.body.classList.toggle('orangeneon', selectedTheme === 'orangeneon');
    });
});
document.addEventListener('DOMContentLoaded', () => {
    const themeSelect = document.getElementById('color-page');

    // Charger les réglages sauvegardés
    const savedTheme = localStorage.getItem('color-page');

    if (savedTheme) {
        document.body.classList.toggle('purpleneon', savedTheme === 'purpleneon');
        themeSelect.value = savedTheme;
    }

    // Appliquer les changements immédiatement quand l'utilisateur change les réglages
    themeSelect.addEventListener('change', () => {
        const selectedTheme = themeSelect.value;
        document.body.classList.toggle('purpleneon', selectedTheme === 'purpleneon');
    });
});
document.addEventListener('DOMContentLoaded', () => {
    const themeSelect = document.getElementById('color-page');

    // Charger les réglages sauvegardés
    const savedTheme = localStorage.getItem('color-page');

    if (savedTheme) {
        document.body.classList.toggle('multineon', savedTheme === 'multineon');
        themeSelect.value = savedTheme;
    }

    // Appliquer les changements immédiatement quand l'utilisateur change les réglages
    themeSelect.addEventListener('change', () => {
        const selectedTheme = themeSelect.value;
        document.body.classList.toggle('multineon', selectedTheme === 'multineon');
    });
});
